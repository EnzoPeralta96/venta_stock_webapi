Prompt 1 — Backend: Paginación y búsqueda de movimientos

  Contexto del proyecto:
  - ASP.NET Core 8, EF Core, PostgreSQL, patrón Repository + Service + Result<T>
  - El módulo de Cuenta Corriente tiene el endpoint GET /api/CurrentAccount/movements/{clientId}
    que actualmente devuelve TODOS los movimientos del cliente sin paginar.
  - El front deriva dos valores críticos de la lista completa:
      opening = movimiento de tipo alta_cliente (IdTipoMovimiento = 2)
      latest  = último movimiento por fecha
    Estos se usan para las cards de resumen (deuda, disponible, saldo a favor).
  - Si paginamos el endpoint actual, el front pierde acceso a opening y latest.
  - Solución: separar en dos endpoints.

  ══════════════════════════════════════════════════════════
  PARTE A — Nuevo endpoint de resumen
  ══════════════════════════════════════════════════════════

  1. Crear DTO:
     Features/CurrentAccount/DTO/MovementDTO/AccountSummaryDTO.cs

     public class AccountSummaryDTO
     {
         public AccountMovementDTO Opening { get; set; }  // movimiento alta_cliente
         public AccountMovementDTO Latest  { get; set; }  // último movimiento por fecha
     }

  2. Agregar método a IAccountMovementRepository:
     Task<(MovimientoCc opening, MovimientoCc latest)> GetAccountSummaryAsync(int clientId);

  3. Implementar en AccountMovementRepository:
     - opening: primer movimiento donde IdTipoMovimiento == 2
     - latest: movimiento con Fecha más reciente (ThenByDescending IdMovimiento como desempate)
     - Ambos con AsNoTracking() e Include de navegaciones necesarias para el mapeo a AccountMovementDTO
       (IdEstadoNavigation, IdTipoMovimientoNavigation, IdUsuarioRegistraNavigation,
        IdVentaNavigation con ThenInclude IdEstadoNavigation)

  4. Agregar método a ICurrentAccountService:
     Task<Result<AccountSummaryDTO>> GetAccountSummaryAsync(int clientId);

  5. Implementar en CurrentAccountService:
     - Verificar que el cliente existe (usar _clientRepository.ExistsByIdAsync)
     - Llamar al repositorio
     - Si opening es null → Failure(CurrentAccountCode.account_not_found)
     - Mapear con AutoMapper a AccountSummaryDTO
     - Retornar Result<AccountSummaryDTO>.Success(...)

  6. Agregar endpoint en CurrentAccountController:
     [Authorize(Policy = "PERM:CC_VIEW")]
     [HttpGet("summary/{clientId}")]
     public async Task<IActionResult> GetAccountSummary(int clientId)

  ══════════════════════════════════════════════════════════
  PARTE B — Endpoint de movimientos paginado con búsqueda
  ══════════════════════════════════════════════════════════

  1. Modificar IAccountMovementRepository:
     Reemplazar la firma actual de GetMovements por:

     Task<PagedList<MovimientoCc>> GetMovementsPagedAsync(
         int clientId,
         int pageIndex,
         int pageSize,
         string searchTerm,
         DateTime? fechaDesde,
         DateTime? fechaHasta);

  2. Implementar en AccountMovementRepository:
     Query base:
     - Filtrar IdCliente == clientId
     - Excluir tipo alta_cliente: IdTipoMovimiento != 2
     - Include de todas las navegaciones (igual que el método anterior)
     - OrderBy Fecha ASC, ThenBy IdMovimiento ASC

     Filtros opcionales (aplicar solo si el parámetro tiene valor):
     - searchTerm: Detalle.Contains(searchTerm)
                   OR (IdVentaNavigation != null && IdVentaNavigation.CodigoVenta.Contains(searchTerm))
     - fechaDesde: Fecha >= fechaDesde
     - fechaHasta: Fecha <= fechaHasta.Value.Date.AddDays(1).AddTicks(-1)
                   (inclusivo hasta el final del día)

     Retornar: PagedList<MovimientoCc>.CreateAsync(query, pageIndex, pageSize)
     Usar AsNoTracking().

  3. Modificar ICurrentAccountService:
     Reemplazar firma de GetAccountMovementsByClientId por:

     Task<Result<PagedList<AccountMovementDTO>>> GetAccountMovementsPagedAsync(
         int clientId,
         int pageIndex,
         int pageSize,
         string searchTerm,
         DateTime? fechaDesde,
         DateTime? fechaHasta);

  4. Implementar en CurrentAccountService:
     - Verificar cliente existe
     - Llamar al repositorio con los parámetros
     - Proyectar con _mapper.Map<List<AccountMovementDTO>>(page.Items) para los items
     - Construir el PagedList<AccountMovementDTO> manualmente (no re-ejecutar la query):
       usar el constructor o factory que acepta items ya mapeados + metadatos de paginación
     - Retornar Result con el PagedList

     Nota sobre el PagedList: si el PagedList del proyecto no tiene constructor que acepte
     items ya resueltos, agregar uno:
     public static PagedList<T> Create(List<T> items, int pageIndex, int pageSize, int totalCount)

  5. Modificar endpoint en CurrentAccountController:
     [Authorize(Policy = "PERM:CC_VIEW")]
     [HttpGet("movements/{clientId}")]
     public async Task<IActionResult> GetAccountMovementsPaged(
         int clientId,
         int pageIndex = 1,
         string searchTerm = "",
         DateTime? fechaDesde = null,
         DateTime? fechaHasta = null)
     {
         int pageSize = 10;
         // llamar a GetAccountMovementsPagedAsync y retornar Ok(result.Value)
     }

  ══════════════════════════════════════════════════════════
  NOTAS IMPORTANTES
  ══════════════════════════════════════════════════════════

  - El endpoint GET /api/CurrentAccount/summary/{clientId} es el único que devuelve
    el movimiento alta_cliente. El endpoint de movements paginado nunca lo incluye.
  - El PagedList ya existe en Shared/Paged/PagedList.cs. Usarlo tal cual o extenderlo
    mínimamente si hace falta el constructor adicional.
  - Mantener el mismo manejo de errores y MessageProvider que el resto del módulo.
  - No eliminar nada del repositorio hasta confirmar que el front no usa el endpoint viejo.

  ---
  Prompt 2 — Backend: ND, NC y anulación de pago via ND

  Contexto del proyecto:
  - ASP.NET Core 8, EF Core, PostgreSQL, patrón Repository + Service + Result<T>
  - El módulo de CC ya tiene RegisterMovement que maneja pagos (pago_global, pago_factura)
    y actualiza MontoPagado en los consumos (movimiento_cc) mediante AllocarPagoGlobal
    y AllocarPagoFactura.
  - Ya existen DebitNoteStrategy (tipo 3) y CreditNoteStrategy (tipo 4) en MovementStrategy.cs.
  - Ya existe GeneratePaymentReceiptAsync que genera PDF con PaymentReceiptDocument.
  - El problema a resolver: cuando un operario usa una ND para corregir un pago registrado
    por error, el MontoPagado en los consumos queda incorrecto. Se necesita un mecanismo
    para recomputar MontoPagado desde cero excluyendo el pago anulado.

  ══════════════════════════════════════════════════════════
  PARTE A — Migración y modelo: campo EsAnulado
  ══════════════════════════════════════════════════════════

  1. Agregar a Models/MovimientoCc.cs:
     public bool EsAnulado { get; set; }

  2. Crear migración EF Core:
     dotnet tool run dotnet-ef migrations add AddEsAnuladoToMovimientoCc

     La migración debe contener SOLO:
     Up:   migrationBuilder.AddColumn<bool>("EsAnulado", "movimiento_cc",
               nullable: false, defaultValue: false);
     Down: migrationBuilder.DropColumn("EsAnulado", "movimiento_cc");

  3. Aplicar la migración:
     dotnet tool run dotnet-ef database update

  ══════════════════════════════════════════════════════════
  PARTE B — DTO: soporte para ND como corrección de pago
  ══════════════════════════════════════════════════════════

  1. Modificar AddMovementDTO:
     Agregar campo opcional:
     public int? IdMovimientoPagoCorregido { get; set; }

     Agregar validación en IValidatableObject.Validate:
     - Si IdMovimientoPagoCorregido tiene valor Y IdTipoMovimiento != 3 (ND):
       yield return ValidationResult("IdMovimientoPagoCorregido solo es válido para Notas de Débito.")

  2. Agregar a AccountMovementDTO:
     public bool EsAnulado { get; set; }

  3. En CurrentAccountProfile, agregar al mapeo MovimientoCc → AccountMovementDTO:
     .ForMember(dest => dest.EsAnulado, opt => opt.MapFrom(src => src.EsAnulado))

  ══════════════════════════════════════════════════════════
  PARTE C — Repositorio: nuevos métodos
  ══════════════════════════════════════════════════════════

  Agregar a IAccountMovementRepository e implementar en AccountMovementRepository:

  1. Task<MovimientoCc> GetPaymentMovementById(int idMovimiento)
     - Busca por IdMovimiento
     - Solo devuelve el movimiento si es de tipo pago_global (6) o pago_factura (8)
     - Sin AsNoTracking (necesitamos tracking para modificarlo)
     - Retorna null si no existe o no es un pago

  2. Task<List<MovimientoCc>> GetAllConsumptionsTracked(int clientId)
     - Devuelve todos los movimiento_cc (IdTipoMovimiento == 5) del cliente
     - Ordenados por Fecha ASC, ThenBy IdMovimiento ASC
     - CON tracking (sin AsNoTracking) para poder modificarlos y guardar

  3. Task<List<MovimientoCc>> GetAllValidPayments(int clientId)
     - Devuelve todos los movimientos de tipo pago_global (6) y pago_factura (8)
       donde EsAnulado == false
     - Ordenados por Fecha ASC, ThenBy IdMovimiento ASC
     - AsNoTracking (solo lectura para el recompute)

  4. Modificar GetPendingSalesForPayment:
     En el GroupJoin que busca movimientos tipo PAGO_FACTURA (8), agregar filtro:
     _context.MovimientoCcs.Where(m => m.IdTipoMovimiento == 8 && !m.EsAnulado)
     (Un pago_factura anulado no debe contar como pago de esa venta)

  5. Modificar GetUnpaidConsumptions (ya existente):
     No requiere cambios — busca consumos con MontoPagado < Importe, que ya
     estará correcto después del recompute.

  ══════════════════════════════════════════════════════════
  PARTE D — Servicio: RegisterMovement + Recompute
  ══════════════════════════════════════════════════════════

  1. En CurrentAccountService.RegisterMovement, después de crear el movimiento,
     agregar el siguiente bloque para ND con corrección de pago:

     if (typeMovement == TypeMovement.NOTA_DEBITO
         && addMovementDTO.IdMovimientoPagoCorregido.HasValue)
     {
         var pagoAAnular = await _accountMovementRepository
             .GetPaymentMovementById(addMovementDTO.IdMovimientoPagoCorregido.Value);

         if (pagoAAnular is null)
             return Result<int>.Failure(CurrentAccountCode.movement_not_found);

         pagoAAnular.EsAnulado = true;
         await _accountMovementRepository.UpdateMovimientos(new List<MovimientoCc> { pagoAAnular });
         await RecomputeMontoPagado(addMovementDTO.IdCliente);
     }

  2. Agregar método privado RecomputeMontoPagado(int clientId):

     private async Task RecomputeMontoPagado(int clientId)
     {
         // 1. Obtener todos los consumos con tracking
         var consumos = await _accountMovementRepository.GetAllConsumptionsTracked(clientId);

         // 2. Resetear MontoPagado a 0 en todos
         foreach (var consumo in consumos)
             consumo.MontoPagado = 0;

         // 3. Obtener todos los pagos válidos (EsAnulado = false), ordenados por fecha ASC
         var pagos = await _accountMovementRepository.GetAllValidPayments(clientId);

         // 4. Re-aplicar allocación para cada pago en orden cronológico
         foreach (var pago in pagos)
         {
             if (pago.IdTipoMovimiento == (int)TypeMovement.PAGO_FACTURA && pago.IdVenta.HasValue)
                 AllocarPagoFacturaEnMemoria(pago.IdVenta.Value, pago.Importe ?? 0, consumos);
             else if (pago.IdTipoMovimiento == (int)TypeMovement.PAGO_GLOBAL)
                 AllocarPagoGlobalEnMemoria(pago.Importe ?? 0, consumos);
         }

         // 5. Guardar todos los consumos actualizados en un batch
         await _accountMovementRepository.UpdateMovimientos(consumos);
     }

  3. Refactorizar AllocarPagoGlobal y AllocarPagoFactura para delegar en versiones
     in-memory (evitar duplicación de lógica):

     // Versión in-memory (trabaja sobre lista ya cargada — para recompute)
     private void AllocarPagoGlobalEnMemoria(decimal importePago, List<MovimientoCc> consumos)
     {
         decimal restante = importePago;
         foreach (var consumo in consumos.OrderBy(c => c.Fecha).ThenBy(c => c.IdMovimiento))
         {
             if (restante <= 0) break;
             decimal pendiente = (consumo.Importe ?? 0) - (consumo.MontoPagado ?? 0);
             if (pendiente <= 0) continue;

             if (restante >= pendiente)
             { consumo.MontoPagado = consumo.Importe; restante -= pendiente; }
             else
             { consumo.MontoPagado = (consumo.MontoPagado ?? 0) + restante; restante = 0; }
         }
     }

     private void AllocarPagoFacturaEnMemoria(int idVenta, decimal importePago, List<MovimientoCc> consumos)
     {
         var consumo = consumos.FirstOrDefault(c => c.IdVenta == idVenta);
         if (consumo is null) return;
         consumo.MontoPagado = Math.Min((consumo.MontoPagado ?? 0) + importePago, consumo.Importe ?? 0);
     }

     // Versiones existentes (para uso normal) llaman a las in-memory + SaveChanges:
     private async Task AllocarPagoGlobal(int clientId, decimal importePago)
     {
         var consumos = await _accountMovementRepository.GetUnpaidConsumptions(clientId);
         if (consumos.Count == 0) return;
         AllocarPagoGlobalEnMemoria(importePago, consumos);
         await _accountMovementRepository.UpdateMovimientos(consumos);
     }

     private async Task AllocarPagoFactura(int idVenta, decimal importePago)
     {
         var consumo = await _accountMovementRepository.GetConsumptionByVentaId(idVenta);
         if (consumo is null) return;
         AllocarPagoFacturaEnMemoria(idVenta, importePago, new List<MovimientoCc> { consumo });
         await _accountMovementRepository.UpdateMovimientos(new List<MovimientoCc> { consumo });
     }

  4. Agregar a CurrentAccountCode (el enum de errores del módulo):
     movement_not_found

     Agregar a CurrentAccountDictionary:
     { CurrentAccountCode.movement_not_found, "El movimiento indicado no existe o no es un pago." }

  ══════════════════════════════════════════════════════════
  PARTE E — PDF adaptado por tipo de movimiento
  ══════════════════════════════════════════════════════════

  1. Modificar PaymentReceiptDataSource:
     Reemplazar el campo TipoPago (string) por:

     public string TipoComprobante { get; set; }  // título del header
     public string ColorHeader     { get; set; }  // "green" | "red" | "blue"

     Eliminar TipoPago si ya no se usa en otro lado.

  2. Modificar PaymentReceiptDocument:
     En ComposeHeader, el color del título y del borde inferior deben venir de _data.ColorHeader:
     - "green" → Colors.Green.Darken2
     - "red"   → Colors.Red.Darken2
     - "blue"  → Colors.Blue.Darken2

     En el bloque derecho del header, mostrar _data.TipoComprobante en lugar del texto hardcodeado.

  3. Modificar GeneratePaymentReceiptAsync en CurrentAccountService:
     Determinar TipoComprobante y ColorHeader según movement.IdTipoMovimiento:

     var (tipoComprobante, colorHeader) = movement.IdTipoMovimiento switch
     {
         6 or 8 => ("COMPROBANTE DE PAGO — CUENTA CORRIENTE",    "green"),
         3      => ("NOTA DE DÉBITO — CUENTA CORRIENTE",         "red"),
         4      => ("NOTA DE CRÉDITO — CUENTA CORRIENTE",        "blue"),
         _      => ("COMPROBANTE DE MOVIMIENTO — CUENTA CORRIENTE", "green")
     };

     Asignar en dataSource:
     dataSource.TipoComprobante = tipoComprobante;
     dataSource.ColorHeader     = colorHeader;

  4. El endpoint GET /api/CurrentAccount/payment-receipt/{idMovimiento} ya existente
     funciona para ND y NC sin cambios — el servicio detecta el tipo y adapta el PDF.
     Solo asegurarse que GetMovementById incluya los datos necesarios para ND/NC
     (el Include de IdClienteNavigation, IdTipoMovimientoNavigation y IdUsuarioRegistraNavigation
     ya está en la implementación actual).

  ══════════════════════════════════════════════════════════
  NOTAS FINALES
  ══════════════════════════════════════════════════════════

  - ND y NC básicos (sin corrección de pago) no requieren ningún cambio adicional:
    ya funcionan vía POST /api/CurrentAccount/register-movement con
    idTipoMovimiento: 3 (ND) o 4 (NC). El frontend solo necesita exponer esos tipos
    en el formulario de ajustes.

  - El campo IdMovimientoPagoCorregido es opcional y SOLO tiene efecto cuando
    idTipoMovimiento == 3 (ND). Para NC u otros tipos, se ignora.

  - EsAnulado se expone en AccountMovementDTO para que el frontend pueda mostrar
    visualmente los pagos anulados en el historial (por ejemplo con un badge gris
    "Anulado" y texto tachado).

  - Registrar en el historial de auditoría la anulación si el proyecto tiene
    interceptor de auditoría activo — esto debería ocurrir automáticamente si
    el interceptor trackea cambios en movimiento_cc.